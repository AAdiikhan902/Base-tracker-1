# Get a free key at https://basescan.org/apis (sign up there directly).
# Basescan's API is now powered by the unified Etherscan V2 system, so this
# same key works when you pass chainid=8453 for Base — no separate key needed.
BASESCAN_API_KEY=

# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
